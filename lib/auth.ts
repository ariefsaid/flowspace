/**
 * Auth.js (NextAuth v5) placeholder.
 *
 * TODO: wire real providers, the Prisma adapter, and session callbacks, then
 * export `handlers`, `auth`, `signIn`, `signOut` from `NextAuth(authConfig)`
 * and mount them in `app/api/auth/[...nextauth]/route.ts`. Kept as a plain
 * config object for now so the scaffold builds without a configured auth route.
 */
import type { NextAuthConfig } from "next-auth";

export const authConfig: NextAuthConfig = {
  providers: [],
};
